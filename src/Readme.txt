To init the serial folder submodule run 
`git submodule init` and
`git submodule update`
To init the libraries not installed by the primary ros installer
'rosdep install --from-paths src --ignore-src -r -y'
